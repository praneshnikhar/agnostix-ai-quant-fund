import { ProviderList } from "@/components/providers/provider-list";

export const metadata = { title: "AI Providers - Agnostix" };

export default function ProvidersPage() {
  return <ProviderList />;
}

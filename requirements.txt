# =============================================================================
# AI Quant Fund — Root Python Requirements
# M0 Foundation
#
# Keep M0 lean. ML/RL dependencies such as PyTorch, XGBoost, LightGBM,
# vectorbt, Transformers, Gymnasium, etc. are intentionally excluded.
# Add them only when their respective milestone begins.
# =============================================================================

# --- Web API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0

# --- Database ---
sqlalchemy[asyncio]>=2.0.30,<3.0
alembic>=1.13,<2.0
asyncpg>=0.29,<1.0

# --- Cache / Task Queue ---
redis>=5.0,<8.0
celery>=5.4,<6.0

# --- HTTP ---
httpx>=0.27,<1.0

# --- Observability ---
structlog>=24.1,<26.0

# --- Agent Orchestration ---
langgraph>=1.0,<2.0

# --- Brokerage / Market Data ---
# Paper trading only during development.
alpaca-py>=0.44,<0.45

# --- Testing ---
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<2.0

# --- Development / Quality ---
ruff>=0.5,<1.0
mypy>=1.10,<2.0